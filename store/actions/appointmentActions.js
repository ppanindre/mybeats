import { generateClient } from "aws-amplify/api";

const client = generateClient();

export const createAppointmentActionCreators = () => async (dispatch, getState) => {
    try {
        
    } catch (error) {
        
    }
};
